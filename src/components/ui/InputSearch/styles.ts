import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    marginLeft: 20,
    height: '100%',
    borderBottomColor: 'white',
    borderBottomWidth: 1,
    color: 'white',
    fontSize: 24
  }
});
